diff --git a/src/fenv/x86_64/fenv.s b/src/fenv/x86_64/fenv.s
index 98d876d..dfff393 100644
--- a/src/fenv/x86_64/fenv.s
+++ b/src/fenv/x86_64/fenv.s
@@ -8,14 +8,14 @@ feclearexcept:
 	test %eax,%ecx
 	jz 1f
 	fnclex
-1:	stmxcsr -8(%rsp)
+1:	.byte 0x0f,0xae,0x5c,0x24,0xf8  # stmxcsr -8(%rsp)
 	and $0x3f,%eax
 	or %eax,-8(%rsp)
 	test %ecx,-8(%rsp)
 	jz 1f
 	not %ecx
 	and %ecx,-8(%rsp)
-	ldmxcsr -8(%rsp)
+	.byte 0x0f,0xae,0x54,0x24,0xf8  # ldmxcsr -8(%rsp)
 1:	xor %eax,%eax
 	ret
 
@@ -23,9 +23,9 @@ feclearexcept:
 .type feraiseexcept,@function
 feraiseexcept:
 	and $0x3f,%edi
-	stmxcsr -8(%rsp)
+	.byte 0x0f,0xae,0x5c,0x24,0xf8  # stmxcsr -8(%rsp)
 	or %edi,-8(%rsp)
-	ldmxcsr -8(%rsp)
+	.byte 0x0f,0xae,0x54,0x24,0xf8  # ldmxcsr -8(%rsp)
 	xor %eax,%eax
 	ret
 
@@ -40,11 +40,11 @@ __fesetround:
 	andb $0xf3,1(%rsp)
 	or %ch,1(%rsp)
 	fldcw (%rsp)
-	stmxcsr (%rsp)
+	.byte 0x0f,0xae,0x1c,0x24  # stmxcsr (%rsp)
 	shl $3,%ch
 	andb $0x9f,1(%rsp)
 	or %ch,1(%rsp)
-	ldmxcsr (%rsp)
+	.byte 0x0f,0xae,0x14,0x24  # ldmxcsr (%rsp)
 	pop %rcx
 	ret
 
@@ -52,7 +52,7 @@ __fesetround:
 .type fegetround,@function
 fegetround:
 	push %rax
-	stmxcsr (%rsp)
+	.byte 0x0f,0xae,0x1c,0x24  # stmxcsr (%rsp)
 	pop %rax
 	shr $3,%eax
 	and $0xc00,%eax
@@ -63,7 +63,7 @@ fegetround:
 fegetenv:
 	xor %eax,%eax
 	fnstenv (%rdi)
-	stmxcsr 28(%rdi)
+	.byte 0x0f,0xae,0x5f,0x1c  # stmxcsr 28(%rdi)
 	ret
 
 .global fesetenv
@@ -73,7 +73,7 @@ fesetenv:
 	inc %rdi
 	jz 1f
 	fldenv -1(%rdi)
-	ldmxcsr 27(%rdi)
+	.byte 0x0f,0xae,0x57,0x1b  # ldmxcsr 27(%rdi)
 	ret
 1:	push %rax
 	push %rax
@@ -81,7 +81,7 @@ fesetenv:
 	pushq $0x37f
 	fldenv (%rsp)
 	pushq $0x1f80
-	ldmxcsr (%rsp)
+	.byte 0x0f,0xae,0x14,0x24  # ldmxcsr (%rsp)
 	add $40,%rsp
 	ret
 
@@ -90,7 +90,7 @@ fesetenv:
 fetestexcept:
 	and $0x3f,%edi
 	push %rax
-	stmxcsr (%rsp)
+	.byte 0x0f,0xae,0x1c,0x24  # stmxcsr (%rsp)
 	pop %rsi
 	fnstsw %ax
 	or %esi,%eax
